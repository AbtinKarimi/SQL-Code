--SELECT COUNT (P_CODE)
--FROM PRODUCT;

--SELECT COUNT (V_CODE)
--FROM PRODUCT;

--SELECT COUNT (*)
--FROM PRODUCT;

--SELECT COUNT (P_PRICE)
--FROM PRODUCT
--WHERE P_PRICE < 10;

--SELECT DISTINCT (V_CODE)
--FROM PRODUCT;

--SELECT COUNT (DISTINCT V_CODE) AS COUNTDISTINCT
--FROM PRODUCT;

--SELECT MAX (P_PRICE) AS MAXPRICE, MIN (P_PRICE) AS MINPRICE
--FROM PRODUCT;

--SELECT MAX (P_INDATE) AS MAXDATE, MIN (P_INDATE) AS MINDATE
--FROM PRODUCT;

--SELECT SUM (CUS_BALANCE) AS TOTALBALANCE
--FROM CUSTOMER;

--SELECT SUM (P_QOH * P_PRICE) AS TOTALVALUE
--FROM PRODUCT;

--SELECT AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT;

--SELECT V_CODE, AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT
--GROUP BY V_CODE
--ORDER BY AVGPRICE DESC;

--SELECT VENDOR.V_CODE, V_NAME, COUNT (P_CODE)AS NUMPRODS, AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT JOIN VENDOR ON PRODUCT.V_CODE = VENDOR.V_CODE
--GROUP BY VENDOR.V_CODE,V_NAME
--ORDER BY V_NAME;

--SELECT VENDOR.V_CODE, V_NAME, SUM (P_QOH) AS TOTALQTY, COUNT (P_CODE)AS NUMPRODS, AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT JOIN VENDOR ON PRODUCT.V_CODE = VENDOR.V_CODE
--GROUP BY VENDOR.V_CODE,V_NAME
--ORDER BY V_NAME;

--SELECT VENDOR.V_CODE, V_NAME, P_QOH, COUNT (P_CODE) AS NUMPRODS, AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT JOIN VENDOR ON PRODUCT.V_CODE = VENDOR.V_CODE
--GROUP BY VENDOR.V_CODE,V_NAME, P_QOH
--ORDER BY V_NAME;

--SELECT VENDOR.V_CODE, V_NAME, V_STATE, COUNT (P_CODE) AS NUMPRODS, AVG (P_PRICE) AS AVGPRICE
--FROM PRODUCT JOIN VENDOR ON PRODUCT.V_CODE = VENDOR.V_CODE
--GROUP BY VENDOR.V_CODE,V_NAME, V_STATE
--ORDER BY V_NAME;

--SELECT V_CODE, COUNT (P_CODE) AS NUMPRODS
--FROM PRODUCT 
--GROUP BY V_CODE
--HAVING AVG (P_PRICE) < 10 
--ORDER BY V_CODE;

--SELECT V.V_CODE, V_NAME, SUM (P_QOH * P_PRICE) AS TOTALPRICE
--FROM PRODUCT AS P JOIN VENDOR AS V ON P.V_CODE = V.V_CODE
--WHERE P_DISCOUNT > 0
--GROUP BY V.V_CODE, V_NAME
--HAVING SUM (P_QOH * P_PRICE) > 500
--ORDER BY SUM (P_QOH * P_PRICE) DESC;

