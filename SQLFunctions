--SELECT P_CODE, CONVERT(VARCHAR(10), P_INDATE, 102)
--FROM PRODUCT;

--SELECT DATEADD(day,90, P_INDATE) AS DueDate
--FROM PRODUCT;

--SELECT DATEDIFF(day, P_INDATE, GETDATE()) AS DaysAgo
--FROM PRODUCT;

--SELECT P_CODE, P_PRICE, ROUND(P_PRICE,1) AS PRICE1, ROUND(P_PRICE,0) AS PRICE0
--FROM PRODUCT;

--SELECT P_PRICE, CEILING(P_PRICE), FLOOR(P_PRICE)
--FROM PRODUCT;

--SELECT CUS_FNAME + ',' + CUS_LNAME AS FULLNAME
--FROM CUSTOMER;

--SELECT UPPER (CUS_FNAME + ',' + CUS_LNAME) AS FULLNAME
--FROM CUSTOMER;

--SELECT LOWER (CUS_FNAME + ',' + CUS_LNAME) AS FULLNAME
--FROM CUSTOMER;

--SELECT CUS_PHONE, SUBSTRING (CUS_PHONE, 1,3) AS PREFIX
--FROM CUSTOMER;

--SELECT CUS_FNAME, CUS_LNAME, LEN (CUS_LNAME) AS LNAMELENGTH
--FROM CUSTOMER;

--SELECT P_CODE, CAST(P_PRICE AS VARCHAR(8)) AS PRICE, 
--CAST(P_INDATE AS VARCHAR(20)) AS INDATE, 
--CAST(P_DISCOUNT AS VARCHAR(4)) AS DISC
--FROM PRODUCT;

--SELECT P_CODE, CONVERT(VARCHAR(8), P_PRICE) AS PRICE, 
--CONVERT(VARCHAR(20), P_INDATE) AS INDATE, 
--CONVERT(VARCHAR(4), P_DISCOUNT) AS DISC
--FROM PRODUCT;

--SELECT V_CODE, V_STATE,
--CASE 
--WHEN V_STATE = 'CA' THEN .08
--WHEN V_STATE = 'FL' THEN .05
--WHEN V_STATE = 'TN' THEN .085
--ELSE 0.00 END AS TAX
--FROM VENDOR;
